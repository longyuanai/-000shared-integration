# 008-M2-OPS 审计报告

> 执行日期：2026-08-07
> 结论：**PASS-WITH-NITS（本地门禁通过，提交/推送/suite CI 待完成）**

## 范围

- 一次性本地 PostgreSQL 16.14 源库与独立恢复库。
- Alembic revision `20260805_0001`。
- 备份/恢复、真实 PostgreSQL 并发、seed 兼容、全量测试和 Gateway smoke。
- 未连接 Render/Neon，未使用生产凭据，未触碰 `003AI Agent安全靶场`。

## OPS-BACKUP-001

| 证据 | 结果 |
|---|---|
| 格式 | `pg_dump --format=custom` / `pg_restore --exit-on-error` |
| PostgreSQL / pg_dump | 16.14 / 16.14 |
| SHA-256 | `cb07327970dfd5dc870cb3ed72f37b1b8b367bc30051a008347164f5ea0dbfdf` |
| 备份大小 | 27,483 bytes |
| 备份耗时 | 220 ms |
| 恢复耗时 | 175 ms |

源库与恢复库比对：

| 项目 | 源库 | 恢复库 |
|---|---:|---:|
| revision | `20260805_0001` | `20260805_0001` |
| tenants | 1 | 1 |
| findings | 7 | 7 |
| jobs | 3 | 3 |
| job_events | 0 | 0 |
| audit_events | 1 | 1 |

两个容器与备份卷均带 `longyuan.m2ops=true` 标签；证据采集后已核对标签并删除，剩余临时容器/卷均为 0。

## OPS-CONCURRENCY-001

`tests/test_postgres_integration.py` 使用真实迁移 PostgreSQL 连续运行三次：

| 轮次 | 结果 | 耗时 |
|---|---|---:|
| 1 | 2 passed | 1.92 s |
| 2 | 2 passed | 2.70 s |
| 3 | 2 passed | 2.43 s |

并发 API key 校验、幂等 Job 创建/claim、Finding 去重和 SQLite 重复导入均通过。

## OPS-SEED-001

真实空迁移库暴露并修复三个运维 seed 漂移：

1. `e2e:` + 64 位哈希超出 `findings.fingerprint VARCHAR(64)`；改为前缀 + 60 位哈希。
2. `seed_e2e.py` 仍使用旧 `JobRow.payload/error` 字段；更新为 `input/queue/progress/error_code/error_message`。
3. SQL fixture 假设租户预先存在；改为事务内幂等创建 `e2e` 测试租户。

验证结果：

- seed 回归：2 passed。
- Python seed 对真实 PostgreSQL 连续两次成功；每次 7 Findings / 3 Jobs，最大指纹长度 64。
- SQL fixture 在全新迁移数据库独立执行成功：1 tenant / 7 Findings / 3 Jobs。

## 全量验证

- Ruff：changed Python files `All checks passed`。
- pytest：`100 passed in 5.20s`，包含真实 PostgreSQL tests。
- admin CLI：`--help` 成功列出运维命令。
- 临时 Gateway：`GET http://127.0.0.1:18080/livez` → HTTP 200，`{"status":"ok"}`；验证后进程已停止。
- 文档仓 `git diff --check`：通过；最终链接审计另见项目交接结果。

## 偏差与剩余门禁

- `inspect_worker_return.py --project 000shared-integration` 不受支持；本次使用全量 pytest、admin CLI 和 Gateway `/livez` 替代，派活文档已校正。
- seed 修复已形成 commit `40eb79c`；本报告与阶段状态按独立 commit 交付。仍需推送、更新 `suite-lock.yml` 并跑成功 suite CI，之后才能标记发布完成。
- Render 私有 GHCR 拉取凭据预计 2026-08-14 到期，仍需轮换或下线服务并撤销。
